import { CheckSquare } from 'lucide-react';
import { BadgeTone } from '@/components/ui/badge';

export const tarefa_config = {
  id: 'tarefa' as const,
  label: 'Tarefas',
  icon: CheckSquare,
  tone: 'info' as BadgeTone,
};
