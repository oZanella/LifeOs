import { Dumbbell } from 'lucide-react';
import { BadgeTone } from '@/components/ui/badge';

export const treino_config = {
  id: 'treino' as const,
  label: 'Treino',
  icon: Dumbbell,
  tone: 'accent' as BadgeTone,
};
